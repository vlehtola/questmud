string time_to_con() {
  return "obj/player"->stime(1015689600-time());
}

status cmd_questcon(string str) {
  int time;
  if(!str || str != "time") {
    write("Try: questcon time\n");
    return 1;
  }
  write(Time to QuestCon 2002 -> "+time_to_con()+"\n");
return 1;
}

