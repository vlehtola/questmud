mapping question;

input_data(string str) {
  if(!question) question = ([ ]);
  if(!str) return 0;
  question[m_sizeof(question)] = str;
  return 1;
}

ask_questions(object ob) {
  if(question
  tell_object(ob, question

}
