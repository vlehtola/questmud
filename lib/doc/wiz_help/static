Help for 'static' command:

SYNTAX
  static set [what] to (number) {player}
  static [arg] {player}

[what]:
  Stats: {str,dex,con,int,wis}
  Points: {hp,sp,ep,max_hp,max_sp,max_ep}
  Other: {silver}

[arg]:  
 'ress': Ress a dead player without any exp lost.
 'heal': Heal hp, sp and slot hp to max.

EXAMPLES
  static set str to 500 celtest
  static set silver to 80 celtest
  static ress celtest
