get_time() {
    return 1;
}
ep_cost() { return 1; }

resolve_skill(str) {
      write("Control animal is an automatic skill.\n");
      return 1;
}

fail_skill(str) {
      write("Control animal is an automatic skill.\n");
      return 1;
}
