*Defined in /sys/macros.h

VBFC(func)	Returns the VBFC string to <func>. Note if you want this
		function to be able to be called outside this object, use
		VBFC_ME since it specifies in what object that function should
		be called.

