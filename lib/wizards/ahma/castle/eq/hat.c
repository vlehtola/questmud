inherit "obj/armour";
start() {
  set_class(5);
  set_value(5000);
  set_sub_type("cap");
  set_name("hat");
  set_short("A tattered mage hat 'Painful Dream'");
  set_long("A mage hat made especially for Demon Archmage Cervos. The cone-shaped dark\n" + 
           "violet hat with thin and large-sized brim has been made from some skin-like,\n" + 
           "soft but durable material. Hat's surface is almost reflecting. A beam-like\n" + 
           "weak orange light pillar rises up from the upper part of the hat's point. Two\n" + 
           "strings made from same material as the actual hat, are hanging from both sides\n" + 
           "of the brim, from near where the wearer's ears are. Strings are almost two feet\n" + 
           "long and they glow in bright orange. The lower end of the point is coated with\n" + 
           "a decorative piece of skin where is slit some runes not familiar to you");
  set_stats("str", -5);
  set_stats("dex", -3);
  set_stats("con", -12);
  set_stats("int", 18);
  set_stats("wis", 11);
  set_stats("spr", 19);
}
