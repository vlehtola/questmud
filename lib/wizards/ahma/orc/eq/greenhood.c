inherit "obj/armour";
start() {
  set_class(4);
  set_sub_type("helm");
  set_name("hood");
  set_short("A hood, green as grass");
  set_long("Perfectly green hood is optimal for sneaking in the forest if you want to\n" +
           "stay unnoticed. It holds rain very well. Hood has been sewn together from\n" +
           "rough fabric, which was probably painted green after the hood was otherwise\n" +
           "complete. It smells of fresh grass");

  set_value(125);
  set_weight(125);
  set_stats("dex", 4);
}
