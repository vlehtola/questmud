inherit "obj/armour";
  start() {
   set_class(1);
   set_name("diary");
   set_alias("book");
   set_short("A old diary");
   set_long("A small book, the only words that you can make out is 'Diary'\n"+
            ", and its painted on the cover of the book.\n");
}