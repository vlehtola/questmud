inherit "obj/armour";
 
start() {
    set_slot("neck");
    set_name("cross");
    set_short("Holy Cross");
    set_long("This is just a normal iron holy symbol in the shape of a cross.\n");
    set_ac(1);
    set_value(3);
    set_weight(100);
}
