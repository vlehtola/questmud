inherit "obj/armour";



reset(arg) {

    ::reset(arg);

    set_name("magical hood of witch");

    set_alias("hood");

    set_short("A magical hood of witch");

    set_long("The witch of the Orthanc used to wear this hood, until it met its destiny. If you concentrate you can still see the face of the witch on the hood. There is something strange on the hood, but you cant say what it's.\n");

    set_ac(3);

    set_value(5000);

    set_weight(300);

    set_stats("int", 2);

    set_stats("wis", 3);

    set_stats("spr", 15);

    set_slot("head");

}

