inherit "obj/armour";

start() {
set_class(7);
set_sub_type("cap");
    set_alias("hat");
    set_short("A straw hat with blue silk ribbon [worn-out]");
    set_long("A worn-out straw hat, with blue silk ribbon tied around the hat.\n"+
             "The blue silk ribbon is keeping the hat all in one heap. On the\n"+
             "top of the hat, can be seen ten huge incisions");

    set_stats("str", 2);
    set_stats("dex", 1);
    set_stats("con", 3);
}

