inherit "obj/armour";

start() {
        set_class(5);
        set_slot("belt");
        set_name("belt_himokeklu");
        set_alias("belt");
        set_short("A small jeweled belt");
        set_long("A small jeweled belt with diamonds embedded on the center "+
                 "of it. It looks very rare and extremely valuable, it is not "+
                 "a magical item but still someone might pay big amount for it.");
        set_weight(200);
        set_value(6000);
}
