short() {return "A small book"; }

long(str) {
     write("There are many pictures on this book. There are\n"+
          "also some mathematical and other problems in it.\n");
return; }

id(str) {return str == "book"; }
get() {
   return 1; }
