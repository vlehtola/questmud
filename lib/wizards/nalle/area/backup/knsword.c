inherit "obj/weapon";

reset(arg) {
    ::reset(arg);
    set_name("sword");
    set_long("A sword used by the Dequan Knights of Chamron.\n");
    set_wc(10);
    set_weapon_type("blade");
    set_value(421);
    set_weight(1);
}
