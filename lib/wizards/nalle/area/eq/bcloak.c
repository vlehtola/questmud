inherit "obj/armour";

start() {
    set_name("cloak");
    set_short("The Eclipse cloak");
    set_long("An extremely beautiful cloak, that seems to conceal its bearer\n" +
             "almost completely.\n");
    set_stats("dex", 5);
    set_weight(200);
    set_value(2000);
    set_slot("cloak");
}
