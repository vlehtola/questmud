inherit "obj/armour";

reset(arg) {
    ::reset(arg);
    set_name("gloves");
    set_short("Blue gloves");
    set_long("These gloves are blue, and they have some strange marks on them.\n");
    set_ac(10);
    set_slot("hands");
    set_value(2000);
    set_stats("spr", 5);
}
