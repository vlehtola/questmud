inherit "obj/armour";

reset(arg) {
    ::reset(arg);
    set_name("robes");
    set_short("Green mage robes");
    set_long("These robes are green. They are made from fine cloth, but otherwise look perfectly normal.\n");
    set_ac(0);
    set_value(2000);
    set_stats("spr", 1);
}
