inherit "obj/armour";

start() {
    set_name("cloak");
    set_short("Cloak of Shadows");
    set_long("This garment looks like a normal cloak made from black silk. Looks like it's only special property is the fact that it reflects no light at all.\n");
    set_stats("con", 7);
    set_slot("cloak");
}
