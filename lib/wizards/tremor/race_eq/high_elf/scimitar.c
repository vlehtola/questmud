inherit "obj/weapon";

reset(arg) {
    ::reset(arg);
set_class(5);
set_sub_type("scimitar");
set_name("sword");
set_short("A scimitar decorated with jewels");
set_long("Scimitar looks extremely valuable due to the amount of\n"+
        "all jewels embedded to the blade and handle of it.\n"+
        "This is no weapon of an ordinary bum. Though it's wildly\n"+
        "decorated, it looks like it still could whup some ass.\n");
}
