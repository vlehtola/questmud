inherit "obj/armour";
 
start() {
set_class(6);
  set_slot("torso");
    set_name("plate");
    set_alias("mail");
    set_short("Hellforge plate");
    set_long("This armour is made of several small pieces of metal, that are bound together by some\n"+
             "sort of magic, since no holes can be seen on it. The mail seems to be able to resist \n"+
             "a great amount of damage, and would certainly protect you from harm.");
    set_value(500);
    set_weight(9);
  
}
