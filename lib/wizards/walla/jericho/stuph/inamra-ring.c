
inherit "obj/armour";
 
start() {
set_class(2);
  set_slot("finger");
    set_name("ring");
    set_alias("ring");
    set_short("Ruby embedded silver ring");
    set_long("This ring is really quite exquisite. The ruby shines in the sun.");
    set_value(10);
    set_weight(1);
 
}
