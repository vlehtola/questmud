 inherit "obj/armour"; 
 
reset(arg) {
    ::reset(arg);
    set_name("belt");
    set_alias("machine");
    set_short("Swedish sucking Machine");
    set_long("The machine is now massaging Lihaskones genitals. You hear moaning from Lihaskone.\n");
    set_ac(1);
    set_value(0);
    set_slot(belt);
  
}
