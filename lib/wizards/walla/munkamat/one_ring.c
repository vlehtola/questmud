inherit "obj/armour";
 
start() {
set_class(1);
set_slot("piercing");
set_name("ring");
set_alias("sormus");
set_short("The one ring to rule them all");
set_long("one ring to rule them all, one ring to find them.\n"+
         "one ring to bind them all and in the darkness bind them.");
set_value(0);
set_weight(1);
}

