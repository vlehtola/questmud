inherit "obj/armour";
 
start() {
set_class(9);
  set_slot("finger");
    set_name("ring");
    set_alias("ring");
    set_short("Ring of Frodo");
    set_long("One ring to rule them all, one ring to bind them. One ring to bring them all, and in the darkness bind them.");
    set_value(5000);
    set_stats("dex",10);
    set_stats("str",10);
    set_stats("int",10);
    set_stats("wis",10);
    set_stats("con",10);
    set_weight(0);
    }
