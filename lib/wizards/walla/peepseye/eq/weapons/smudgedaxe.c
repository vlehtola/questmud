inherit "obj/weapon"; 
 
start() {
  set_class(10);
  set_sub_type("great axe");
    set_short("A Double sided smudgy axe of leader");
    set_long("the axes blades are a bit rusty but still when you look at it it seems to glow green a bit.\n" +
   "When you swing axe trough air it leaves a light traces(uran) behind as if your movement \n" +
   "accelerates when holding this axe.\n");
  set_stats("damage", 3);
  set_stats("hit", 1);
  set_stats("hpr", 1);
  set_stats("spr", 1);
 
}
