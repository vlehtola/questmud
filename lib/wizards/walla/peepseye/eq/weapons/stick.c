
inherit "obj/weapon";

reset(arg) {
    ::reset(arg);
    set_name("stick");
    set_short("Pepe's walking stick with a sword embedded in it");
    set_long("Pepe's walking stick is carved with skulls and ships and runes. Looks just\n" +
"like a real pirates sword.\n");
    set_weapon_type("blade");
    set_value(690);
    set_weight(210);

}

