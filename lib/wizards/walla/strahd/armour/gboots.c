inherit "obj/armour";

reset(arg) {
    ::reset(arg);
    set_name("boots");
    set_alias("boot");
    set_short("Guards stinking boots");
    set_long("Guards must have stepped into some shit to have their boots smell like this.\n");
    set_material(2);
    set_ac(10);
    set_value(69);
    set_weight(1);
    set_slot("boots");

}
