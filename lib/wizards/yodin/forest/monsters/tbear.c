inherit "obj/monster";
reset(arg) {
    ::reset(arg);
    if(arg) { return; }
    set_level(150);
    set_name("bear");
    set_alias("bear");
    set_short("A huge bear crowling here.(FRIEND OF HUMAN BOT KILLER)");
    set_long("Rusty bot killer. Tweek tweek skriiiks poks.\n");
   

}
